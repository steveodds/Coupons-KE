@model IEnumerable<CouponsKE.Models.TrackedPrice>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DesiredPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LowestPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StoreWithLowestPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DesiredPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LowestPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StoreWithLowestPrice)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TrackedPriceID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TrackedPriceID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TrackedPriceID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
